/**
 * Enum required by [SearchAccountPayload]{@link ../interfaces/SearchAccountPayload.html} if searching for a postcode and house number.
 */
export enum AddressTypes {
    SUPPLY = 'delivery',
    CORRESPONDENCE = 'correspondence'
}
